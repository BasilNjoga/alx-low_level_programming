#include "main.h"

/**
 * print_most_numbers - prints all numbers
 *
 * Return: Always (0) - success
 */

void print_most_numbers(void)
{
	int num;
	char ch;

	for (num = 0; num <= 9; num++)
	{
	_putchar(num);
	}
	ch = '\n';
	_putchar (ch);
}
