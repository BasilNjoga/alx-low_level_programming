#include "main.h"

/**
 * print_numbers - prints all numbers
 *
 * Return: Always (0) - success
 */

void print_numbers(void)
{
	char num;

	for (num = 48; num <= 57; num++)
	{
	_putchar(num);
	}
	_putchar('\n');
}
