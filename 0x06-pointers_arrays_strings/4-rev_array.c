#include <main.h>

/**
 * reverse_array - gives the reverse of an array
 *
 * @a the array;
 *
 * @n - number of elemens
 *
 * Return:nothing
 */

void reverse_array(int *a, int n)
{
	int i;
	int len;

	len = sizeof(a) / sizeof(int) ;
	}
