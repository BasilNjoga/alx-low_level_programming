#include "main.h"
#include "string.h"

/**
 * strlen - returns the length
 *
 * @s: string to be measured
 *
 * Return: Always 0 - success
 */

int _strlen(char *s)
{
	int stringlen;

	stringlen = strlen(s);
	return (stringlen);
}
