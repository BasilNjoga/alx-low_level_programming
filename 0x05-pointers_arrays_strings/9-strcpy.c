#include "main.h"

/**
 * -strcpy - copies one string to another
 *
 * @dest: buffer string
 *
 * @src: source string
 *
 * Return: Always 0 - Success
 */

char *_strcpy(char *dest, char *src)
{
	int len;
	int i;

	for (i = 0; i <= len; i++)
	{
	dest[i] = src[i] ;
	}

	return (dest);
}
